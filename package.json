{
  "name": "aify-wrapper",
  "version": "0.6.2",
  "description": "Harness wrappers: launchers that resolve a coding-agent CLI, export an identity environment, point it at an MCP bridge, and exec it.",
  "license": "MIT",
  "bin": {
    "aify-wrapper-install": "./install.sh",
    "aify-wrapper-check": "./bin/aify-wrapper-check.mjs"
  },
  "files": [
    "wrappers/",
    "lib/",
    "bin/",
    "docs/",
    "render.sh",
    "install.sh",
    "VERSION",
    "README.md"
  ],
  "scripts": {
    "test": "node tests/run-in-a-temp-root.mjs"
  }
}
